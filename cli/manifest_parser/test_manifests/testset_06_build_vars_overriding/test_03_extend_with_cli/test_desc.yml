build_vars:
  # Override LIB4_VAR2 which *is* defined by the app as well
  LIB4_VAR2: lib4_var2_from_cli
  # This one is not defined anywhere.
  NEW_VAR_FROM_CLI: new_from_cli
  # This one has default value but is overridden.
  OVERRIDE_FROM_CLI: overridden_from_cli
