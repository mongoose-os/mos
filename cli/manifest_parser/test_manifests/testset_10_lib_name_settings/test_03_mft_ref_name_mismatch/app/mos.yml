manifest_version: 2020-08-02

libs:
  - location: libs/mylib1
    name: lib1a

# A library in use has `name' set in the manifest referring to it that doesn't
# correspond to the basename of its location.  The same is done in the
# manifest referencing the library.  Check that the mismatch of these two names
# set is properly detected and reported.
