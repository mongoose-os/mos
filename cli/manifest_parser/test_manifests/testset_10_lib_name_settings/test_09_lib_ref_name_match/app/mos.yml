manifest_version: 2020-08-02

libs:
  - location: libs/mylib1
    name: lib1

# prepareLib() commentary case (9): a library in use has same `name' set in its
# and referring manifests that is distinct from its location basename.  Check
# that the generated MGOS_HAVE_* and mgos_*_init() identifiers are properly
# altered.
