manifest_version: 2020-08-02

libs:
  - location: libs/mylib1
    name: mylib1

# prepareLib() commentary case (8): a library in use has `name' set in its
# manifest distinct from `name' set in the referring manifest.  Check that this
# mismatch is properly detected and reported.
