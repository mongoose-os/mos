fwbuild:
  hosts:
    mongoose.cloud
  vars:
    ansible_become: yes
    ansible_ssh_user: core
    ansible_become_user: root

website:
  hosts:
    mongoose-os.com
  vars:
    ansible_become: yes
    ansible_ssh_user: core
    ansible_become_user: root
    ansible_python_interpreter: /root/bin/python
